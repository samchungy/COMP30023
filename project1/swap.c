#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv){
  int time_cr;
  int pr_id;
  int mem_size;
  int job_time;

  while(scanf("%d",&time_cr)!=EOF){
    scanf("%d", &pr_id);
    scanf("%d", &mem_size);
    scanf("%d", &job_time);
    /*printf("%d, %d, %d, %d\n",time_cr, pr_id, mem_size, job_time);*/
  }
  return 0;
}
